---
import PostHog from "./PostHog.astro";
import "../styles/global.css";

const title = "Local AI Benchmarks";
const basePath = import.meta.env.BASE_URL;
const subtitle = "a side quest by eeshans.com";
const isStaticBuild = process.env.ASTRO_OUTPUT === "static";
const iconStyle = (name: string) => `--icon: url('${basePath}assets/icons/${name}.svg')`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href={`${basePath}favicon.svg`} />
    <PostHog />
    <script is:inline>
      try {
        const savedTheme = localStorage.getItem("theme");
        if (savedTheme === "dark") {
          document.documentElement.dataset.theme = "dark";
        }
      } catch {}
    </script>
  </head>
  <body data-workspace="visual" data-base-path={basePath} data-static-build={isStaticBuild ? "true" : undefined}>
    <div class="app-shell">
      <header class="app-header">
        <div class="app-header-inner mx-auto flex min-h-16 w-full max-w-7xl items-center gap-4 px-4 sm:px-6 lg:px-8">
          <div class="header-title flex min-w-0 flex-1 items-center gap-3">
            <div class="min-w-0">
              <h1 class="truncate-line text-base font-semibold tracking-[-0.01em]">
                Local AI Benchmarks
              </h1>
              <p class="muted-copy truncate-line text-sm">a side quest by <a href="https://eeshans.com/" target="_blank" rel="noopener" class="header-subtitle-link"><strong>eeshans.com</strong></a></p>
            </div>
          </div>

          <div class="header-actions flex shrink-0 items-center gap-2">
            <a class="btn-sm-outline source-pill" href="https://github.com/eeshansrivastava89/local-llm-visual-benchmark" target="_blank" rel="noopener noreferrer" aria-label="View source on GitHub">
              <span class="ui-icon" aria-hidden="true" style={iconStyle("github")}></span>
              <span>Source</span>
            </a>
            <span class="badge-outline machine-pill gap-1.5 text-xs" id="statsPill" data-tooltip="Machine profile loading." tabindex="0" role="status" aria-label="Machine profile">
              <span class="ui-icon" aria-hidden="true" style={iconStyle("sparkles")}></span>
              <span id="statsDot" class="status-dot" data-state="checking"></span>
              <span id="statsCompact">Machine</span>
            </span>
            <button type="button" class="btn-sm-outline icon-only-btn" id="themeToggle" aria-label="Use dark theme" title="Use dark theme">
              <span id="themeIcon" aria-hidden="true">☾</span>
              <span class="sr-only" id="themeLabel">Dark</span>
            </button>
            <button type="button" class="btn operational-control" id="runToggle"><span class="ui-icon" aria-hidden="true" style={iconStyle("plus")}></span>Prepare run</button>
          </div>
        </div>
      </header>

      <main class="mx-auto w-full max-w-7xl px-4 py-4 sm:px-6 lg:px-8 md:py-5">
        <div class="grid gap-4">
          <!-- Onboarding -->
          <div id="onboardingPanel" class="onboarding-panel" hidden>
            <div class="onboarding-header">
              <h2 class="text-sm font-semibold">Welcome — 3 steps to your first benchmark</h2>
              <button type="button" class="btn-sm-ghost" id="dismissOnboarding">Dismiss</button>
            </div>
            <ol class="onboarding-steps">
              <li data-onboarding-step="1"><span class="step-num">1</span><code>local-llm setup</code> — create a model profile</li>
              <li data-onboarding-step="2"><span class="step-num">2</span><code>local-llm prepare</code> — pick a benchmark and harness</li>
              <li data-onboarding-step="3"><span class="step-num">3</span>Generate and capture your result</li>
            </ol>
          </div>

          <!-- Kind tabs -->
          <div class="kind-bar">
            <div class="kind-tabs">
              <button type="button" class="kind-tab" data-kind="visual" aria-pressed="true"><span class="ui-icon" aria-hidden="true" style={iconStyle("eye")}></span>Visual Benchmark</button>
              <button type="button" class="kind-tab" data-kind="data-science" aria-pressed="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("chart-bar")}></span>Data Science Benchmark</button>
            </div>
          </div>

          <!-- Toolbar -->
          <div class="toolbar">
            <div class="view-tabs">
              <button type="button" class="view-tab" data-mode="model" aria-pressed="true"><span class="ui-icon" aria-hidden="true" style={iconStyle("boxes")}></span>By model</button>
              <button type="button" class="view-tab" data-mode="benchmark" aria-pressed="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("panels-top-left")}></span>By prompt</button>
              <button type="button" class="view-tab" data-mode="compare" aria-pressed="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("table-2")}></span>Compare</button>
            </div>
            <div class="toolbar-filters">
              <button type="button" class="btn-sm-outline filters-toggle" id="filtersToggle" aria-expanded="false" aria-controls="toolbarFilterGroup">
                <span class="ui-icon" aria-hidden="true" style={iconStyle("settings")}></span>
                <span>Filters</span>
                <span class="filters-toggle-count" id="filtersToggleCount" hidden>0</span>
              </button>
              <div class="toolbar-filter-group" id="toolbarFilterGroup">
                <div class="dropdown-wrap">
                  <select id="modelFilter" class="input text-sm" aria-label="Filter by model" disabled data-loading="true">
                    <option value="all">All models</option>
                  </select>
                </div>
                <div class="dropdown-wrap">
                  <select id="benchmarkFilter" class="input text-sm" aria-label="Filter by prompt" disabled data-loading="true">
                    <option value="all">All prompts</option>
                  </select>
                </div>
                <div class="dropdown-wrap">
                  <select id="harnessFilter" class="input text-sm" aria-label="Filter by harness" disabled data-loading="true">
                    <option value="all">All harnesses</option>
                  </select>
                </div>
                <input
                  id="runsSearch"
                  class="input text-sm"
                  type="search"
                  aria-label="Search runs"
                  placeholder="Search runs, models, prompts, harnesses"
                />
              </div>
              <button type="button" class="btn-sm-outline operational-control" id="refreshRuns" data-icon="refresh-cw"><span class="ui-icon" aria-hidden="true" style={iconStyle("refresh-cw")}></span>Refresh</button>
            </div>
          </div>

          <!-- Summary -->
          <div class="gallery-summary">
            <div class="min-w-0">
              <div class="flex items-center gap-2">
                <h2 id="viewTitle" class="text-lg font-semibold tracking-[-0.01em]">Prompt comparison</h2>
                <span class="badge-outline" id="runCount">0</span>
              </div>
              <p class="muted-copy mt-0.5 text-sm" id="viewSubtitle">{subtitle}</p>
              <p class="muted-copy text-sm" id="runSummary">Loading saved runs.</p>
            </div>
            <div class="summary-actions">
              <label class="cloud-toggle" id="cloudToggleLabel">
                <input type="checkbox" id="cloudModelsToggle" />
                <span class="cloud-toggle-text">Include cloud models</span>
              </label>
              <button type="button" class="btn-sm-outline gallery-reset" id="clearWorkbenchState" data-icon="x" hidden><span class="ui-icon" aria-hidden="true" style={iconStyle("x")}></span><span id="clearWorkbenchStateLabel">Clear filters</span></button>
            </div>
          </div>

          <!-- Surface -->
          <div id="runsSurface"></div>
        </div>
      </main>

      <div id="helpTooltip" class="help-tooltip-layer" role="tooltip" hidden></div>

      <footer class="footer" role="contentinfo">
        <span>&copy; 2025&ndash;2026 Eeshan Srivastava</span>
        <span>Personal project &middot; MIT License &middot; Non-commercial</span>
        <span class="footer-links">
          <a href="https://github.com/eeshansrivastava89/local-llm-visual-benchmark" target="_blank" rel="noopener noreferrer">Source</a>
          <a href="https://www.linkedin.com/in/eeshans/" target="_blank" rel="noopener noreferrer">LinkedIn</a>
        </span>
      </footer>
    </div>

    <!-- Visual Detail Modal -->
    <div class="detail-backdrop" id="detailBackdrop">
      <article class="detail-shell" role="dialog" aria-modal="true" aria-label="Run detail">
        <header class="detail-header">
          <div class="min-w-0">
            <h2 id="detailTitle" class="truncate-line text-lg font-semibold">Run detail</h2>
            <p class="muted-copy truncate-line mt-1 text-sm" id="detailSubtitle"></p>
          </div>
          <div class="flex flex-wrap items-center justify-end gap-2">
            <button type="button" class="btn-sm-outline operational-control" id="openHtml" data-icon="file-code" data-operational-available="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("file-code")}></span>Open HTML</button>
            <button type="button" class="btn-sm-outline operational-control" id="copyDetailPath" data-icon="copy" data-operational-available="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("copy")}></span>Copy path</button>
            <button type="button" class="btn-sm-outline operational-control" id="openRunFolder" data-icon="folder-open" data-operational-available="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("folder-open")}></span>Open in Finder</button>
            <button type="button" class="btn-sm-outline operational-control" id="recaptureRun" data-icon="camera" data-operational-available="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("camera")}></span>Recapture media</button>
            <button type="button" class="btn-sm-danger operational-control" id="deleteRun" data-icon="trash-2" data-operational-available="false"><span class="ui-icon" aria-hidden="true" style={iconStyle("trash-2")}></span>Delete run</button>
            <button type="button" class="btn-sm-outline" id="closeDetail"><span class="ui-icon" aria-hidden="true" style={iconStyle("x")}></span>Close</button>
          </div>
        </header>
        <div class="detail-body p-5">
          <div class="grid min-w-0">
            <div id="detailPreview"></div>
          </div>
          <aside class="detail-inspector">
            <div class="run-identity">
              <div class="meta-grid" id="detailMeta"></div>
            </div>
            <section class="inspector-section">
              <div class="section-row">
                <h3 id="detailTextTitle">Prompt</h3>
                <div class="section-actions">
                  <span class="badge-outline" id="promptLength">0 chars</span>
                  <button type="button" class="btn-sm-outline" id="copyDetailPrompt" data-icon="copy"><span class="ui-icon" aria-hidden="true" style={iconStyle("copy")}></span>Copy prompt</button>
                </div>
              </div>
              <pre class="code-block prompt-block" id="detailPrompt"></pre>
            </section>

          </aside>
        </div>
      </article>
    </div>

    <!-- Delete Confirmation Modal -->
    <div class="detail-backdrop" id="deleteConfirmBackdrop">
      <article class="confirm-shell" role="dialog" aria-modal="true" aria-label="Delete run">
        <header class="detail-header">
          <div class="min-w-0">
            <h2 class="truncate-line text-lg font-semibold">Delete run?</h2>
            <p class="muted-copy truncate-line mt-1 text-sm">This removes the run folder from disk.</p>
          </div>
          <button type="button" class="btn-sm-outline" id="closeDeleteConfirm"><span class="ui-icon" aria-hidden="true" style={iconStyle("x")}></span>Close</button>
        </header>
        <div class="grid gap-4 p-5">
          <div class="confirm-warning-box">
            <p class="confirm-warning-title">This cannot be undone.</p>
            <p class="muted-copy text-sm leading-5">The whole run folder will be deleted, including metadata, prompt, HTML, preview, and raw response files.</p>
          </div>
          <div class="grid gap-1">
            <span class="section-label">Run folder</span>
            <code class="code-inline-path" id="deleteRunPath"></code>
          </div>
          <div class="flex flex-wrap justify-end gap-2">
            <button type="button" class="btn-sm-outline" id="cancelDeleteRun"><span class="ui-icon" aria-hidden="true" style={iconStyle("x")}></span>Cancel</button>
            <button type="button" class="btn-sm-danger" id="confirmDeleteRun"><span class="ui-icon" aria-hidden="true" style={iconStyle("trash-2")}></span>Delete folder</button>
          </div>
        </div>
      </article>
    </div>

    <!-- Chart Lightbox -->
    <div class="detail-backdrop" id="chartLightbox" role="dialog" aria-modal="true" aria-label="Chart viewer">
      <button type="button" class="chart-lightbox-close" id="closeChartLightbox" aria-label="Close">&times;</button>
      <button type="button" class="chart-lightbox-nav chart-lightbox-prev" id="chartLightboxPrev" aria-label="Previous chart">&lsaquo;</button>
      <img id="chartLightboxImg" src="" alt="" />
      <button type="button" class="chart-lightbox-nav chart-lightbox-next" id="chartLightboxNext" aria-label="Next chart">&rsaquo;</button>
      <p class="chart-lightbox-label" id="chartLightboxLabel"></p>
    </div>

    <!-- How to Prepare a Run -->
    <div class="detail-backdrop" id="prepBackdrop">
      <article class="prep-shell" role="dialog" aria-modal="true" aria-label="How to prepare a run">
        <header class="detail-header">
          <div class="min-w-0">
            <h2 class="truncate-line text-lg font-semibold">How to prepare a run</h2>
            <p class="muted-copy truncate-line mt-1 text-sm">Use the CLI to set up models, prepare run directories, and launch benchmarks.</p>
          </div>
          <button type="button" class="btn-sm-outline" id="closePrep"><span class="ui-icon" aria-hidden="true" style={iconStyle("x")}></span>Close</button>
        </header>
        <div class="prep-body p-5">
          <div class="deprecation-notice">
            <div class="cli-commands mt-0">
              <div class="cli-command">
                <div class="cli-cmd-row"><code class="cli-cmd">local-llm setup</code><button type="button" class="cli-copy-btn" data-copy="local-llm setup" title="Copy command"><span class="ui-icon" aria-hidden="true" style={iconStyle("copy")}></span></button></div>
                <span class="muted-copy text-sm">Create a model profile — llama.cpp, Ollama, or oMLX</span>
              </div>
              <div class="cli-command">
                <div class="cli-cmd-row"><code class="cli-cmd">local-llm prepare</code><button type="button" class="cli-copy-btn" data-copy="local-llm prepare" title="Copy command"><span class="ui-icon" aria-hidden="true" style={iconStyle("copy")}></span></button></div>
                <span class="muted-copy text-sm">Pick a benchmark, model source, and harness → creates the run directory with prompt</span>
              </div>
              <div class="cli-command">
                <div class="cli-cmd-row"><code class="cli-cmd">local-llm run &lt;profile&gt; --with pi</code><button type="button" class="cli-copy-btn" data-copy="local-llm run <profile> --with pi" title="Copy command"><span class="ui-icon" aria-hidden="true" style={iconStyle("copy")}></span></button></div>
                <span class="muted-copy text-sm">Start the model server and launch your harness</span>
              </div>
            </div>
            <div class="cli-flow mt-5">
              <h4 class="text-sm font-semibold">Typical flow</h4>
              <ol class="cli-steps mt-2">
                <li><strong>Setup</strong> — <code>local-llm setup</code> — pick a backend and model, save a profile</li>
                <li><strong>Prepare</strong> — <code>local-llm prepare</code> — choose benchmark + model source + harness, get a run directory and prompt</li>
                <li><strong>Run</strong> — <code>local-llm run &lt;profile&gt; --with pi</code> — starts the server and fires up your coding tool</li>
              </ol>
            </div>
            <a class="btn mt-5" href="https://github.com/eeshansrivastava89/local-llm-visual-benchmark#local-llm-cli" target="_blank" rel="noopener noreferrer"><span class="ui-icon" aria-hidden="true" style={iconStyle("github")}></span>CLI docs</a>
          </div>
        </div>
      </article>
    </div>

    <script is:inline type="module" src={`${basePath}viewer.js`}></script>
  </body>
</html>
